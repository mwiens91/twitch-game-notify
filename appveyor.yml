image: ubuntu

install:
  - sudo apt install -y python3-pip libdbus-1-dev libdbus-glib-1-dev
  - pip3 install -r requirements.txt

build: off

test_script:
  # Test
  - ./run_twitchgamenotify.py --print-to-terminal
